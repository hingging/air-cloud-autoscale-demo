# Air Cloud Autoscale Demo

Static HTML demo for showing how agent traffic is distributed across APN worker nodes and how Air Cloud scales workers as workload increases.

Open `index.html` directly in a browser, or serve this folder with any static file server.
